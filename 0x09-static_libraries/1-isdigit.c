/**
 * _isdigit - checkes if character entered is a digit fro 0 to 9
 * @c: receives the number
 *
 * Return: 1 if tge numver is a digit from 0 to 9 and 0 otherwise
 */
int _isdigit(int c)
{
	return (c <= '9' && c >= '0');
}
